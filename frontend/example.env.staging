# Change the values and change the name of the file to '.env.staging'
# to use it for your configuration
# Used with npm run staging, to verify changes, in a different api url, mainly locally

# Backend communication config values
# If for example you will communicate with http://localhost:3000/api
VITE_API_PORT=3000
VITE_API_URL=http://localhost

# DON'T CHANGE
VITE_USING_ENV=1